import { useState } from "react";
export function SectionLabel({ children }) { return <p style={{ fontSize:11, letterSpacing:"3px", textTransform:"uppercase", color:"#059669", fontWeight:700, marginBottom:10 }}>{children}</p>; }
export function SectionTitle({ children }) { return <h2 style={{ fontFamily:"Syne,sans-serif", fontSize:"clamp(30px,4vw,48px)", fontWeight:800, marginBottom:48, letterSpacing:"-0.5px", color:"#0f172a" }}>{children}</h2>; }
export function Green({ children }) { return <span style={{ color:"#059669" }}>{children}</span>; }
export function Btn({ children, onClick, primary }) { const [hovered, setHovered] = useState(false); return <button onClick={onClick} onMouseEnter={() => setHovered(true)} onMouseLeave={() => setHovered(false)} style={{ background:primary?(hovered?"#047857":"#059669"):(hovered?"#ecfdf5":"transparent"), color:primary?"#fff":"#059669", padding:"14px 34px", borderRadius:8, fontWeight:700, fontSize:14, letterSpacing:".5px", border:primary?"none":`2px solid ${hovered?"#059669":"rgba(5,150,105,.4)"}`, cursor:"pointer", fontFamily:"'DM Sans',sans-serif", transform:hovered?"translateY(-2px)":"translateY(0)", transition:"all .2s", boxShadow:primary?"0 8px 20px rgba(5,150,105,.25)":"none" }}>{children}</button>; }
