import { useEffect, useState } from "react";
import { Green, SectionLabel, SectionTitle } from "../components/ui";
import { PROJECTS } from "../data/portfolio";
export function Projects() { const [selected, setSelected] = useState(null); const featured = PROJECTS.filter((project) => project.featured); const rest = PROJECTS.filter((project) => !project.featured); return <section id="projects" style={{ padding:"100px 6%", background:"#fff" }}><SectionLabel>What I've Built</SectionLabel><SectionTitle>My <Green>Projects</Green></SectionTitle><div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fit,minmax(340px,1fr))", gap:24, marginBottom:52 }}>{featured.map((project) => <ProjectCard key={project.name} project={project} featured onOpen={() => setSelected(project)} />)}</div><p style={{ fontSize:12, letterSpacing:2, textTransform:"uppercase", color:"#8296a3", fontWeight:700, marginBottom:20 }}>More Projects</p><div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fit,minmax(270px,1fr))", gap:20 }}>{rest.map((project) => <ProjectCard key={project.name} project={project} onOpen={() => setSelected(project)} />)}</div>{selected && <ProjectModal project={selected} onClose={() => setSelected(null)} />}</section>; }
function ProjectCard({ project, featured, onOpen }) { const [hovered, setHovered] = useState(false); return <article onClick={onOpen} onMouseEnter={() => setHovered(true)} onMouseLeave={() => setHovered(false)} style={{ background:"#fff", border:`1px solid ${hovered?`${project.accentColor}66`:"rgba(5,150,105,.12)"}`, borderRadius:featured?22:18, overflow:"hidden", cursor:"pointer", transform:hovered?"translateY(-6px)":"none", boxShadow:hovered?"0 20px 40px rgba(15,23,42,.12)":"0 4px 16px rgba(15,23,42,.06)", transition:"all .25s" }}><div style={{ height:featured?200:160, background:project.bgColor, display:"flex", alignItems:"center", justifyContent:"center", fontSize:featured?72:52, position:"relative" }}><span style={{ position:"absolute", top:12, right:12, fontSize:10, fontWeight:700, color:project.accentColor, background:"#fff", padding:"4px 10px", borderRadius:20 }}>{project.link?"🔗 LIVE":"PROJECT"}</span><span>{project.icon}</span></div><div style={{ padding:featured?26:18 }}><h3 style={{ fontFamily:"Syne,sans-serif", fontSize:featured?21:16, fontWeight:800, marginBottom:12 }}>{project.name}</h3><div style={{ display:"flex", flexWrap:"wrap", gap:6 }}>{project.tech.slice(0, featured?project.tech.length:3).map((tech) => <span key={tech} style={{ fontSize:11, padding:"4px 11px", borderRadius:20, background:project.bgColor, color:project.accentColor, fontWeight:600 }}>{tech}</span>)}{!featured && project.tech.length > 3 && <span style={{ fontSize:10, padding:"3px 10px", borderRadius:20, background:"#f1f5f9", color:"#8296a3" }}>+{project.tech.length - 3}</span>}</div></div></article>; }
function ProjectModal({ project, onClose }) { useEffect(() => { const close = (event) => event.key === "Escape" && onClose(); document.addEventListener("keydown", close); document.body.style.overflow = "hidden"; return () => { document.removeEventListener("keydown", close); document.body.style.overflow = ""; }; }, [onClose]); return <div onClick={(event) => event.target === event.currentTarget && onClose()} style={{ position:"fixed", inset:0, background:"rgba(15,23,42,.45)", backdropFilter:"blur(6px)", zIndex:500, display:"flex", alignItems:"center", justifyContent:"center", padding:20 }}><div style={{ background:"#fff", borderRadius:24, padding:36, maxWidth:580, width:"100%", maxHeight:"85vh", overflowY:"auto", position:"relative", animation:"fadein .2s ease" }}><button aria-label="Close project details" onClick={onClose} style={{ position:"absolute", top:16, right:16, border:0, background:"#f1f5f9", width:34, height:34, borderRadius:"50%", cursor:"pointer" }}>✕</button><div style={{ fontSize:52 }}>{project.icon}</div><h3 style={{ fontFamily:"Syne,sans-serif", fontSize:24, margin:"16px 0 14px" }}>{project.name}</h3>{project.link && <a href={project.link} target="_blank" rel="noreferrer" style={{ display:"inline-block", marginBottom:24, color:"#059669" }}>View Live ↗</a>}<ul style={{ listStyle:"none", display:"grid", gap:14 }}>{project.points.map((point) => <li key={point} style={{ display:"flex", gap:12, color:"#5b6b78" }}><span style={{ color:"#059669" }}>→</span>{point}</li>)}</ul></div></div>; }
