import { useState } from "react";
import { SectionLabel, SectionTitle, Green } from "../components/ui";
import { SKILLS, SKILL_FILTERS, TAG_STYLES } from "../data/portfolio";
export function Skills() { const [active, setActive] = useState("all"); const filtered = active === "all" ? SKILLS : SKILLS.filter((skill) => skill.tag === active); return <section id="skills" style={{ padding:"100px 6%", background:"linear-gradient(180deg,#fff,#f7faf9)" }}><SectionLabel>What I know</SectionLabel><SectionTitle>Skills & <Green>Technologies</Green></SectionTitle><div style={{ display:"flex", gap:10, flexWrap:"wrap", marginBottom:40 }}>{SKILL_FILTERS.map((filter) => <button key={filter.key} onClick={() => setActive(filter.key)} style={{ padding:"8px 20px", borderRadius:24, cursor:"pointer", fontFamily:"DM Sans,sans-serif", fontSize:13, fontWeight:600, border:active === filter.key?"1px solid #059669":"1px solid rgba(5,150,105,.2)", background:active === filter.key?"#ecfdf5":"#fff", color:active === filter.key?"#047857":"#5b6b78" }}>{filter.label}</button>)}</div><div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fill,minmax(130px,1fr))", gap:16 }}>{filtered.map((skill) => <SkillCard key={skill.name} skill={skill} />)}</div></section>; }
function SkillCard({ skill }) { const [hovered, setHovered] = useState(false); const tag = TAG_STYLES[skill.tag]; return <div onMouseEnter={() => setHovered(true)} onMouseLeave={() => setHovered(false)} style={{ background:"#fff", border:`1px solid ${hovered?"rgba(5,150,105,.4)":"rgba(5,150,105,.12)"}`, borderRadius:16, padding:"22px 12px", display:"flex", flexDirection:"column", alignItems:"center", gap:10, transform:hovered?"translateY(-5px)":"none", boxShadow:hovered?"0 12px 28px rgba(15,23,42,.09)":"0 1px 3px rgba(15,23,42,.04)", transition:"all .22s" }}><div style={{ fontSize:28 }}>{skill.icon}</div><div style={{ fontSize:13, fontWeight:500, color:"#0f172a", textAlign:"center" }}>{skill.name}</div><span style={{ fontSize:10, padding:"3px 10px", borderRadius:20, fontWeight:700, background:tag.bg, color:tag.color }}>{skill.tagLabel}</span></div>; }
